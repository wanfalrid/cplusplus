#include <iostream>
#include <cmath>

using namespace std;

int main ()
{
    double v = 3.14;
    double x = 3.99;
    double y = 4;
    double z;

    // z = max (x, y); // untuk menunjukan mana yg lebih besar antara x dan y
    // z = min (x, y); // untuk menunjukan mana yang lebih kecil antara x dan y
    // z = pow (2, 3); // untuk akar pangkat. jadi 2 pangkat 3, yang hasilnya 8
    // z = sqrt (9); // untuk akar kuadrat. jadi akar kuadrat dari 9 adalah 3
    // z = abs (-3); // membuat bilangan negatif menjadi positif. abs = absolut
    // v = round (v); // pembulatan sebuah angka. 3.14 jadi 3. 3.51 jadi 4
    // v = ceil (v); // pembulatan ke atas. semisal 3.14 jadi 4
     x = floor (x); //pembulatan ke bawah. semisal 3.99 jadi 3
    
    
    cout << x; // << jangan Lupa variabel nya diubah

    return 0;
}